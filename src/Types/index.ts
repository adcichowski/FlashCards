export interface UserData {
  email: string;
  password: string;
}
export interface ModalInterface {
  text: string;
  isLoading: boolean;
}
export interface GeneralCard {
  language: string;
  answer: string;
  question: string;
  isFavorite: boolean;
}
