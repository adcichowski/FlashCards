{
  "name": "server",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "docker": "docker run -d --rm --name postgres -e POSTGRES_USER=user -e POSTGRES_PASSWORD=pass -e -p 5432:5432 postgres",
    "start": "tsnd --respawn src/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "pre-commit": "echo 'Server precommit running'"
  },
  "author": "Adam Cichowski",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^3.10.0",
    "body-parser": "^1.19.2",
    "express": "^4.17.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.21",
    "prisma": "^3.10.0",
    "ts-node": "^10.7.0",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.6.2"
  }
}
